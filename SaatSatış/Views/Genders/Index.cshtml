@model IEnumerable<Models.Gender>

@{
    ViewBag.Title = "Cinsiyet";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Cinsiyetler</h2>

<p>
    @Html.ActionLink("Yeni Oluştur", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GenderName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GenderName)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { id=item.GenderID }) |
            @Html.ActionLink("Detaylar", "Details", new { id=item.GenderID }) |
            @Html.ActionLink("Sil", "Delete", new { id=item.GenderID })
        </td>
    </tr>
}

</table>
